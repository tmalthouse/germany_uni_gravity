# Python / tooling
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.DS_Store

# Data: sources are imported once into data/raw (see `make import-raw`);
# everything under interim/ and processed/ is rebuilt by make.
/data/raw/
/data/interim/
/data/processed/

# Build outputs are regenerated by `make`
/output/

# LaTeX build files
/paper/*.aux
/paper/*.bbl
/paper/*.bcf
/paper/*.blg
/paper/*.fdb_latexmk
/paper/*.fls
/paper/*.log
/paper/*.out
/paper/*.pdf
/paper/*.run.xml
/paper/*.synctex.gz
